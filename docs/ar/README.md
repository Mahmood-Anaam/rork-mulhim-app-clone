# تطبيق ملهم — توثيق شامل

> تطبيق لياقة بدنية ذكي وثنائي اللغة (عربي/إنجليزي) مبني بـ React Native وExpo وSupabase وOpenAI

## نظرة عامة

**ملهم** هو تطبيق لياقة بدنية مدعوم بالذكاء الاصطناعي، مصمم للمستخدمين الناطقين بالعربية. يقدم:

- 🏋️ **خطط تمرين شخصية** مُولَّدة بناءً على ملف تعريف المستخدم وأهدافه
- 🥗 **خطط تغذية** مبنية على الأطعمة السعودية التقليدية
- 🤖 **مدرب ذكاء اصطناعي** — محادثة تفاعلية مع OpenAI
- 📊 **تتبع التقدم** — تسجيل الوزن وحساب الإنجازات
- 🌐 **دعم ثنائي اللغة** — واجهة كاملة بالعربية (RTL) والإنجليزية

## المكدس التقني

| الطبقة | التقنية |
|--------|---------|
| إطار العمل | React Native + Expo (SDK 54) |
| التوجيه | Expo Router v6 |
| الواجهة الخلفية | Supabase (PostgreSQL + RLS) |
| الذكاء الاصطناعي | OpenAI عبر `@rork-ai/toolkit-sdk` |
| إدارة الحالة | React Context + Zustand + React Query |
| التخزين المحلي | AsyncStorage |
| اللغة | TypeScript |

## بدء سريع

```bash
# 1. استنساخ المستودع
git clone <رابط_المستودع>
cd rork-mulhim-app-clone

# 2. تثبيت المتطلبات
bun install

# 3. إنشاء ملف البيئة
cp .env.example .env   # ثم أضف مفاتيحك

# 4. تشغيل التطبيق
bun run start      # Expo DevTools
bun run web        # معاينة في المتصفح
bun run ios        # محاكي iOS
bun run android    # محاكي Android
```

## الميزات الرئيسية

### خطة التمرين
- يختار الذكاء الاصطناعي قالب التمرين المناسب (Full Body / Upper-Lower / PPL)
- يصفي التمارين حسب موقع التدريب (صالة / منزل / معدات محدودة) والإصابات
- يتكيف مع الهدف (خسارة وزن / بناء عضلات / لياقة عامة)
- تتبع إتمام التمارين

### التغذية
- استبيان تكرار الأغذية لتقييم العادات الغذائية
- خطة وجبات أسبوعية تلقائية بأطباق سعودية تقليدية
- توليد قائمة التسوق من الخطة
- اقتراحات وجبات مخصصة بالذكاء الاصطناعي

### مدرب الذكاء الاصطناعي
- محادثة فورية مع استجابات متدفقة
- اقتراحات مدركة للسياق بناءً على ملف المستخدم
- حفظ التمارين والوجبات المقترحة في الخطة مباشرة

### المصادقة
- تسجيل الدخول بالبريد/كلمة المرور عبر Supabase Auth
- وضع الضيف (بدون حساب، تخزين محلي)
- مزامنة تلقائية عند تسجيل الدخول

## هيكلية المشروع

```
├── app/                  # شاشات Expo Router
│   ├── (tabs)/           # تبويبات التنقل الرئيسية
│   ├── auth/             # تسجيل الدخول والتسجيل
│   └── ...               # شاشات أخرى
├── components/           # مكونات UI قابلة لإعادة الاستخدام
│   ├── ui/               # مكونات مشتركة
│   ├── plan/             # مكونات خطة التمرين
│   ├── nutrition/        # مكونات التغذية
│   ├── coach/            # مكونات المدرب
│   └── profile/          # مكونات الملف الشخصي
├── hooks/                # Hooks مخصصة
├── providers/            # Context providers
├── lib/                  # خدمات خارجية
├── types/                # تعريفات TypeScript
├── constants/            # الألوان والترجمات
├── data/                 # بيانات ثابتة (تمارين، وجبات)
├── backend/              # خادم Hono/tRPC
├── docs/                 # التوثيق
└── supabase-migration.sql  # مخطط قاعدة البيانات
```

## روابط التوثيق

- [دليل الإعداد](SETUP.md) — التثبيت والإعداد
- [المعمارية](ARCHITECTURE.md) — تصميم الكود وتدفق البيانات
- [الشاشات](SCREENS.md) — توثيق شاشات التطبيق

---

*مبني بـ ❤️ باستخدام [Rork.com](https://rork.com)*
